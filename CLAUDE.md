# CLAUDE.md

## Structure

This is a monorepo with two main areas:

- `content/` — Baha'i educational modules (markdown, research, drafts)
- `web/` — Publishing website (when created)

## Context routing

- Working on module content → read `content/CLAUDE.md` for rules and routing
- Working on the web app → read `web/README.md` (when created)
